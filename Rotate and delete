class Solution {
    public static int rotateDelete(ArrayList<Integer> arr) {
        int n = arr.size();
        int temp = n/2;
        int i = 3*(temp/2);
        
        if(temp%2 != 0)
        i++;
        
        return arr.get(n-i);
    }
}
